{
  "name": "crown-of-words",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:syntax": "node --check src/main.js && node --check src/game/campaign-data.js && node --check src/game/campaign-core.js && node --check scripts/check-assets.mjs",
    "check:assets": "node scripts/check-assets.mjs",
    "test:core": "node --test src/game/campaign-core.test.mjs",
    "test": "npm run check:syntax && npm run check:assets && npm run test:core && npm run build"
  },
  "devDependencies": {
    "vite": "8.2.0"
  }
}
