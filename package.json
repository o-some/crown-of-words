{
  "name": "crown-of-words",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:syntax": "node --check src/main.js && node --check src/game/campaign-data.js && node --check src/game/campaign-core.js && node --check src/game/learning-core.js && node --check src/game/kai-core.js && node --check src/game/save-contract.js && node --check src/adapters/standalone-storage.js && node --check src/content/garden-content.js && node --check scripts/check-assets.mjs && node --check tests/e2e/branch5-playtest.cjs",
    "check:assets": "node scripts/check-assets.mjs",
    "test:campaign": "node --test src/game/campaign-core.test.mjs",
    "test:learning": "node --test src/game/learning-core.test.mjs",
    "test:kai": "node --test src/game/kai-core.test.mjs",
    "test:save": "node --test src/game/save-contract.test.mjs",
    "test": "npm run check:syntax && npm run check:assets && npm run test:campaign && npm run test:learning && npm run test:kai && npm run test:save && npm run build"
  },
  "devDependencies": {
    "vite": "8.2.0"
  }
}
